sudo apt install unzip curl -y


curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"

unzip awscliv2.zip
sudo ./aws/install
aws s3 cp index.html s3://jenkins-github-artifacts/













sudo apt install docker.io -y

sudo systemctl start docker

sudo systemctl enable docker

docker version

sudo usermod -aG docker ubuntu

newgrp docker

docker images

sudo systemctl status jenkins

sudo usermod -aG docker jenkins

sudo systemctl restart jenkins









pipeline {

 agent any

 

 stages {

 

  stage('Clone GitHub Repository') {

   steps {

    git branch : 'main', url: 'https://github.com/FETAKE/demo_s3.git'

   }

  }

 

  stage('Run Docker Container') {

   steps {

    sh 'docker rm -f helloworld || true'

    sh 'docker run -d -p 80:80 --name helloworld -v $PWD:/usr/local/apache2/htdocs httpd'

   }

  }

 

 }

}





